\RequirePackage{luatex85}
\documentclass[tikz]{standalone}

\usepackage{polyglossia}
\usepackage{fontspec}
\usepackage{csquotes}

\setdefaultlanguage{czech}
\setmainfont{TeX Gyre Termes}

\usetikzlibrary{decorations.pathreplacing}

\begin{document}

\begin{tikzpicture}
	\node (url) at (0,0) {Vzorek};
	\node (feature) at (4,0) {\( x = \left\{ \begin{array}{c} ( x_{1, 1}, \dots, x_{1,d} ) \\ ( x_{2, 1}, \dots, x_{2, d} ) \\ \vdots \\ ( x_{b, 1}, \dots, x_{b, d}) \end{array} \right\} \)};
	\node (classifier) at (9,0) {\( f(x) = \begin{cases} +1 \\ -1 \end{cases} \)};
	\draw [->] (url.east) -- (feature.west) node [pos=0.5,above] {extrakce};
	\draw [->] (url.east) -- (feature.west) node [pos=0.5,below] {příznaků};
	\draw [->] (feature.east) -- (classifier.west) node [pos=0.5,above] {klasifikace};
\end{tikzpicture}

\end{document}
